node_modules/
.next/
out/
build/
# Generated by `npm run snapshot` from data/*.csv. Derived, never a source —
# committing it would put a second copy of the log in the repo, which is the
# one thing this project has consistently refused to do.
snapshot.html
.DS_Store
*.pem
npm-debug.log*
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
